## 统一异步忙碌状态的最新实践（2025-02-18）
- Modal 按钮在处理绑定/解绑等操作时，统一使用 `runWithBusy` 管理禁用、文本和错误提示
- 如需阻止多次并发点击，可在外层继续保留 `isBusy` 等局部状态作为互斥锁
- `runWithBusy` 的 `errorContext` 字段需填入 `<组件名>.<方法名>`，以便集中排查日志
- 默认忙碌文案使用 `处理中…`，确保与其他 UI 一致